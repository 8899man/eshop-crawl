<h1><%= title '商品分类' %></h1>
<ul class="inline">
  <% @categories.each do |category| %>
    <li><%= link_to category, category_wine_monitors_path(category) %>(<%= WineMonitor.tagged_with_on(:categories, category).count %>)</li>
  <% end -%>
</ul>

